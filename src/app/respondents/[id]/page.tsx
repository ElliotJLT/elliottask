/**
 * The respondent record is rendered by the shell so it can stay mounted, and
 * keep its exact dimensions, when an interview opens beside it.
 */
export default function RespondentPage() {
  return null;
}
